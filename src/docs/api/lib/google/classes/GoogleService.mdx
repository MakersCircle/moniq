[**moniq-app**](../../../index.mdx)

***

[moniq-app](../../../index.mdx) / [lib/google](../index.mdx) / GoogleService

# Class: GoogleService

Defined in: [lib/google.ts:47](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L47)

Unified Google API Client & Auth Service.
Handles authentication, token refreshing, and raw requests to Drive and Sheets APIs.

## Constructors

### Constructor

> **new GoogleService**(): `GoogleService`

#### Returns

`GoogleService`

## Methods

### copyFile()

> **copyFile**(`fileId`, `folderId`, `newName`): `Promise`\<`string`\>

Defined in: [lib/google.ts:239](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L239)

Copy a file to a specific folder with a new name.
Used for creating tiered backups.

#### Parameters

##### fileId

`string`

##### folderId

`string`

##### newName

`string`

#### Returns

`Promise`\<`string`\>

***

### createFolder()

> **createFolder**(`name`): `Promise`\<`string`\>

Defined in: [lib/google.ts:222](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L222)

Create a new folder in Google Drive.

#### Parameters

##### name

`string`

#### Returns

`Promise`\<`string`\>

The created folder ID.

***

### deleteFile()

> **deleteFile**(`fileId`): `Promise`\<`void`\>

Defined in: [lib/google.ts:273](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L273)

Permanent deletion of a file by ID.

#### Parameters

##### fileId

`string`

#### Returns

`Promise`\<`void`\>

***

### driveRequest()

> **driveRequest**(`path`, `options?`): `Promise`\<`Response`\>

Defined in: [lib/google.ts:190](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L190)

Wrapper for raw requests to the Google Drive v3 API.

#### Parameters

##### path

`string`

The endpoint path starting with / (e.g. /files)

##### options?

`RequestInit` = `{}`

#### Returns

`Promise`\<`Response`\>

***

### fetch()

> **fetch**(`url`, `options?`): `Promise`\<`Response`\>

Defined in: [lib/google.ts:125](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L125)

Centralized fetch for Google APIs.
Handles Authorization header, 401 detection, and automatic silent refresh retry.

#### Parameters

##### url

`string`

The full URL to request.

##### options?

`RequestInit` = `{}`

Standard RequestInit options.

#### Returns

`Promise`\<`Response`\>

The fetch Response object.

***

### fetchUserProfile()

> **fetchUserProfile**(): `Promise`\<[`GoogleUserProfile`](../interfaces/GoogleUserProfile.mdx)\>

Defined in: [lib/google.ts:180](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L180)

Fetches the profile of the currently authenticated Google user.

#### Returns

`Promise`\<[`GoogleUserProfile`](../interfaces/GoogleUserProfile.mdx)\>

***

### findFolder()

> **findFolder**(`name`): `Promise`\<`string` \| `null`\>

Defined in: [lib/google.ts:208](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L208)

Find a folder by name in the user's Google Drive.

#### Parameters

##### name

`string`

#### Returns

`Promise`\<`string` \| `null`\>

Folder ID or null if not found.

***

### listFiles()

> **listFiles**(`folderId`, `prefix?`): `Promise`\<[`GoogleDriveFile`](../interfaces/GoogleDriveFile.mdx)[]\>

Defined in: [lib/google.ts:256](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L256)

List files in a specific folder, optionally matching a name prefix.
Results are ordered by creation time descending.

#### Parameters

##### folderId

`string`

##### prefix?

`string`

#### Returns

`Promise`\<[`GoogleDriveFile`](../interfaces/GoogleDriveFile.mdx)[]\>

***

### sheetsRequest()

> **sheetsRequest**(`path`, `options?`): `Promise`\<`Response`\>

Defined in: [lib/google.ts:198](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L198)

Wrapper for raw requests to the Google Sheets v4 API.

#### Parameters

##### path

`string`

The endpoint path starting with / (e.g. /spreadsheets)

##### options?

`RequestInit` = `{}`

#### Returns

`Promise`\<`Response`\>

***

### silentRefresh()

> **silentRefresh**(): `Promise`\<`string` \| `null`\>

Defined in: [lib/google.ts:71](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L71)

Attempts a silent token refresh using prompt: 'none'.
Requires that the user is already logged in to Google in the browser.
This is used to maintain the session without interrupting the user.

#### Returns

`Promise`\<`string` \| `null`\>

The new access token or null if refresh failed.

***

### getInstance()

> `static` **getInstance**(): `GoogleService`

Defined in: [lib/google.ts:55](https://github.com/MakersCircle/moniq/blob/406d49afc56445ed613f3d820cf7ac3043c2d2fb/src/lib/google.ts#L55)

Returns the singleton instance of the GoogleService.

#### Returns

`GoogleService`
